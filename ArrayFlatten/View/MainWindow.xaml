<Window x:Class="ArrayFlatten.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArrayFlatten"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
        <TextBox Text="{Binding InputArrayString, UpdateSourceTrigger=PropertyChanged}"  Width="200"/>
        <TextBox Text="{Binding OutputArrayString, UpdateSourceTrigger=PropertyChanged}"  Width="200"/>
        <Button Grid.Row="1" Content="Flatten" Command="{Binding CheckCommand}" Width="200"/>
    </StackPanel>
</Window>
